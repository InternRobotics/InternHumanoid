# @package _global_

robot:
  control:
        clip_action: 100.0
        control_type: "P" # P: position, V: velocity, T: torques
        # PD Drive parameters:
        default_damping_ratio: 0.05
        stiffness: {"hip": 100, "knee": 100, "ankle": 20, "waist": 500, "shoulder": 20, "elbow": 20, "wrist": 50} # [N*m/rad]
        damping: {"waist": 3, "wrist": 0.5}  # [N*m*s/rad]
        action_names: ["hip", "knee", "ankle", "waist", "shoulder", "elbow"]
        smooth_action_names: ["waist", "shoulder", "elbow"]
        action_scale: 0.25
        # decimation: Number of control action updates @ sim DT per policy DT
        decimation: 4
  asset:
        file: "./resources/robots/G1/urdf/g1_29dof.urdf"
        name: "g1_29dof"  # actor name
        
        # body & keyframe setting
        keyframe_name: "keyframe"
        feet_contact_binding: {"left_ankle": "left_ankle_roll_link", "right_ankle": "right_ankle_roll_link",}
        trunk_names : ["pelvis", "torso", "head"]  # navigation
        mobile_names: ["elbow", "wrist"]  # locomotion
        marker_names: ["knee", "ankle"]  # manipulation
        endeffector_names: ["head", "knee", "ankle", "elbow", "wrist"]
        
        payload_name: "torso_link"
        perturb_body_names: ["torso_link", "ankle_roll_link", 
                              "wrist_roll_rubber_hand", "wrist_yaw_link"]
        terminate_after_contacts_on: []
        penalised_contacts_on: ["torso_link"]
        
        # Merge bodies connected by fixed joints.
        # Specific fixed joints can be kept by adding " <... dont_collapse="true">
        collapse_fixed_joints: True
        disable_gravity: False
        fix_base_link: False # fixe the base of the robot
        default_dof_drive_mode: 3 # see GymDofDriveModeFlags (0 is none, 1 is pos tgt, 2 is vel tgt, 3 effort)
        self_collisions: 0 # 1 to disable, 0 to enable...bitwise filter
        replace_cylinder_with_capsule: True # replace collision cylinders with capsules, leads to faster/more stable simulation
        flip_visual_attachments: False # Some .obj meshes must be flipped from y-up to z-up
        
        density: 0.001
        angular_damping: 0.0
        linear_damping: 0.0
        max_angular_velocity: 1000.0
        max_linear_velocity: 1000.0
        armature: 0.0
        thickness: 0.01